"""
Конфигурация загружается из .env файла.
Создайте .env в корне проекта и добавьте туда необходимые переменные.
"""
import os
from pathlib import Path
from dotenv import load_dotenv

# Загружаем переменные из .env файла
env_path = Path(__file__).parent.parent / ".env"
load_dotenv(dotenv_path=env_path)

# Пользовательский токен (для чтения комментариев)
USER_TOKEN = os.getenv("USER_TOKEN", "")

# Токен сообщества (для ответов на комментарии)
GROUP_TOKEN = os.getenv("GROUP_TOKEN", "")

# ID группы (без минуса)
GROUP_ID = int(os.getenv("GROUP_ID", "0"))

# Номер поста из формата wall-223693021_<POST_ID>
POST_ID = int(os.getenv("POST_ID", "0")) if os.getenv("POST_ID") else None

# Текст ответа, который уйдёт каждому участнику
PROMO_MESSAGE = os.getenv("PROMO_MESSAGE", "Спасибо за участие в розыгрыше! 🎉\nЛови промокод: PROMO2025 ❤️")

# Задержка между запросами чтобы не попадать в лимиты
REQUEST_DELAY = float(os.getenv("REQUEST_DELAY", "0.35"))